const Footer = () => {
  // 设置样式
  const footerStyle = {
    backgroundColor: "#eecbd6",
    fontWeight: "bold", // 设置文字粗体
    color: "white", // 设置文字颜色为白色
    fontSize: "16px", // 可选：设置字体大小
    textAlign: "center", // 水平居中
    padding: "10px 0", // 可选：为顶部和底部添加适当的间距
  };

  return <div style={footerStyle}>Footer</div>;
};

export default Footer;
